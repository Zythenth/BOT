# Environment files

Use this folder only for examples and documentation.

Copy one of the example files to a real `.env` file at the project root before running the bot locally:

```txt
env/.env.development.example -> .env
```

Never commit real Discord tokens, GIPHY keys, database files, or private server IDs.
